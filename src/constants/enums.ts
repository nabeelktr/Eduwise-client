export enum CoursePage {
    courseInformation = 0,
    courseData = 1,
    courseContent = 2,
    coursePreview = 3
}

export enum EISidebar{
    createCourse = 0,
    allCourse = 1,
    editCourse = 2,
    uploadMedia = 3,
}

export enum ProfileSidebar {
    profileInfo = 1,
    changePassword = 2,
}